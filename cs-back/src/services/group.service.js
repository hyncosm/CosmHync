const mongoose = require("mongoose");
const { Group } = require("../models");
const Groups = mongoose.model("Group");

const addGroup = () => {};

const alterGroup = () => {};

const deleteGroup = () => {};
